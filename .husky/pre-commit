#!/bin/sh

# 型チェック
echo "🔍 型チェックを実行中..."
deno check src/main.ts || exit 1

# リンター
echo "🎨 リンターを実行中..."
deno lint || exit 1

# テスト
echo "🧪 テストを実行中..."
deno task test || exit 1 